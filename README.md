# smartdispatch-ai-engine
The Python-based AI/ML Optimization Engine. This repo contains all the code for dynamic pricing, driver matching, route optimization, demand prediction, and fraud detection. It should expose its functionalities via its own API (e.g., using Flask or FastAPI) that the smartdispatch-backend can consume.
